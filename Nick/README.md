## Label_with_LLMs

- Notebooks for labelling datasets by prompting generative models through APIs.

## misc_scripts

- *data_generation_tests_and_tries*  
  Test and development scripts used for synthetic data generation.

- *FINE_TUNING_PEFT_BUG-hard_labels_were_not_shifted_in_training_in _every_script_here*  
  Scripts and notebooks for LoRA adaptation of LLM models (needs debugging).

- *roberta-with-separator_diff_setups_BUG-hard_labels_were_not_shifted_in_training*  
  Scripts for for training token classifiers with alternative training set-up (needs debugging).


## roberta-with-separator-soft-labelling

- An adaptation of scripts found in `../Xinyan` to use `model_input` + special token `<@@>` + `model_output_text` in training.


## submissions_and_results

- Labelled datasets, IoU and Correlation scores, scripts to visualise the scores and to process the submission platform website information.